# Bia'net Admin Panel

Web frontend for admin dashboard, KYC queue, and user management (Vite, port 5175).

## Quick Start

```bash
npm install
npm run dev
```

Requires the backend API at `http://localhost:8080`.
